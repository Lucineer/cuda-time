[package]
name = "cuda-time"
version = "0.1.0"
edition = "2021"
description = "Time utilities — monotonic clocks, duration math, scheduling, time windows, deadlines, and temporal context for agent reasoning"

[dependencies]
serde = { version = "1", features = ["derive"] }
